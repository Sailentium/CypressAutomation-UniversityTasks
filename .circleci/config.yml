version: 2
jobs:
  build:
    docker:
      - image: circleci/node:16.0-browsers
    working_directory: ~/MoviesProject
workflows:
  build:
    jobs:
      - cypress/install:
      
      - cypress/run:
          requires:
            - cypress/install
          start: npm start
          wait-on: "http://localhost:3000"
